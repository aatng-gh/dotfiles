# television: Ctrl-R history, Ctrl-T smart autocomplete
(( $+commands[tv] )) && eval "$(tv init zsh)"
(( $+commands[starship] )) && eval "$(starship init zsh)"
(( $+commands[zoxide] )) && eval "$(zoxide init zsh)"
