# Zsh TODO

- On each host, migrate its `~/.zshenv` bootstrap to set `ZDOTDIR` and source
  `zsh/.zshenv`; preserve any host-specific XDG overrides.
- Manually exercise the Skim history, file, and directory widgets in a real
  terminal after changing their UI or bindings.
